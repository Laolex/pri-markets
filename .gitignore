fhevmTemp/
node_modules/
artifacts/
cache/
typechain-types/
.env
.env.*
frontend/node_modules/
frontend/dist/
frontend/.vercel/
*.log
.DS_Store
.vercel
.env*

# Commit the env template (must come after the .env* ignores — last match wins).
!.env.example

# Agent goal-ledger (execution memory, not product code)
.agent/
